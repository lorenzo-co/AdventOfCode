pub fn process_part1(input: &str) -> String {
    let rows = input
        .lines()
        .map(|line| {
            let max_height = line.chars().max().unwrap();
            let 
        });

    "a".to_string()
}

pub fn process_part2(input: &str) -> String {
    "a".to_string()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = process_part1("a");
        assert_eq!(result, "a");
    }
}
